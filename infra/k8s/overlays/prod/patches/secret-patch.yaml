# Replace with External Secrets / sealed-secrets in real deployments.
apiVersion: v1
kind: Secret
metadata:
  name: cqrs-demo-secrets
stringData:
  MSSQL_SA_PASSWORD: "REPLACE_WITH_PRODUCTION_SECRET"
