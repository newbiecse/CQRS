apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cqrs-demo

resources:
  - ../../base

patches:
  - path: patches/secret-patch.yaml
    target:
      kind: Secret
      name: cqrs-demo-secrets
