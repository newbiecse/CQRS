# Backward-compatible entrypoint — same as dev stack.
# Prefer: scripts/start-infra.ps1 -Environment dev|staging|prod

include:
  - path: base/docker-compose.yml
  - path: env/dev/docker-compose.override.yml
